<!DOCTYPE html>
<html>
  <head>
    <title>DrugNote</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="wrapper">
      <div class="header">
        <div class="left_header">
          <div class="header-name">
            <a href="#">お薬手帳</a>
          </div>
        </div>
        <div class="right_header">
          <div class="greeting">
            <a href="#">名前</a>
          </div>
        </div>
      </div>
      <div class="home">
        <div class="home_left">
          <div class>
            <ul class="lists">
              <li class="list">マイページ</li>
              <li class="list">お薬保存</li>
            </ul>
          </div>
        </div>
        <div class="home_right">
        </div>
      </div>
    </div>

  <body>
    <%#header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= "お薬手帳" %>
          <%#/div>
          <%# ログインしているときは以下を表示するようにしましょう %>
          <%# if user_signed_in? %>
            <%#div class="nav__right"> 
              <%#= link_to "ログアウト",  class: :nav__logout %>
              <%#= link_to "New Proto",  class: :nav__btn %>
            <%#/div> 
          <%# // ログインしているときは上記を表示するようにしましょう %>
          <%# else %>
          <%# ログインしていないときは以下を表示するようにしましょう %>
            <%#div class="nav__right">
              <%#= link_to "ログイン", class: :nav__btn %>
              <%#%= link_to "新規登録", class: :nav__btn %>
            <%#/div>
          <%# end %>
          <%# // ログインしていないときは上記を表示するようにしましょう %>
        <%#/div>
      </div>
    </header>
    <div class="wrapper">
      <div class="side-bar">
      <div class="home">
  </body>
</html>

<%#= "こんにちは、" %>
        <%#= link_to "#{current_user.name}さん", user_path(current_user), method: :get, class: :greeting__link%>